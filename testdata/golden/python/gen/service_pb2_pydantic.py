"""Generated by proto2type from service.proto. DO NOT EDIT."""

from __future__ import annotations


from pydantic import BaseModel, ConfigDict, Field
from .user_pb2_pydantic import User


class GetUserRequest(BaseModel):
    """GetUserRequest is the request for GetUser."""
    model_config = ConfigDict(populate_by_name=True)

    id: str = ''


def apply_field_mask_get_user_request(dst: GetUserRequest, src: GetUserRequest, paths: list[str]) -> None:
    """Copies fields from src to dst based on the given paths."""
    import copy
    for path in paths:
        if path == "id":
            dst.id = src.id

class GetUserResponse(BaseModel):
    """GetUserResponse is the response for GetUser."""
    model_config = ConfigDict(populate_by_name=True)

    user: User | None = None


def apply_field_mask_get_user_response(dst: GetUserResponse, src: GetUserResponse, paths: list[str]) -> None:
    """Copies fields from src to dst based on the given paths."""
    import copy
    for path in paths:
        if path == "user":
            dst.user = copy.deepcopy(src.user)

class ListUsersRequest(BaseModel):
    """ListUsersRequest is the request for ListUsers."""
    model_config = ConfigDict(populate_by_name=True)

    page_size: int = 0
    page_token: str = ''


def apply_field_mask_list_users_request(dst: ListUsersRequest, src: ListUsersRequest, paths: list[str]) -> None:
    """Copies fields from src to dst based on the given paths."""
    import copy
    for path in paths:
        if path == "page_size":
            dst.page_size = src.page_size
        elif path == "page_token":
            dst.page_token = src.page_token

class ListUsersResponse(BaseModel):
    """ListUsersResponse is the response for ListUsers."""
    model_config = ConfigDict(populate_by_name=True)

    users: list[User] = Field(default_factory=list)
    next_page_token: str = ''


def apply_field_mask_list_users_response(dst: ListUsersResponse, src: ListUsersResponse, paths: list[str]) -> None:
    """Copies fields from src to dst based on the given paths."""
    import copy
    for path in paths:
        if path == "users":
            dst.users = copy.deepcopy(src.users)
        elif path == "next_page_token":
            dst.next_page_token = src.next_page_token

class UpdateUserRequest(BaseModel):
    """UpdateUserRequest is the request for UpdateUser."""
    model_config = ConfigDict(populate_by_name=True)

    user: User | None = None
    update_mask: list[str] = Field(default_factory=list)


def apply_field_mask_update_user_request(dst: UpdateUserRequest, src: UpdateUserRequest, paths: list[str]) -> None:
    """Copies fields from src to dst based on the given paths."""
    import copy
    for path in paths:
        if path == "user":
            dst.user = copy.deepcopy(src.user)
        elif path == "update_mask":
            dst.update_mask = copy.deepcopy(src.update_mask)


GetUserRequest.model_rebuild()
GetUserResponse.model_rebuild()
ListUsersRequest.model_rebuild()
ListUsersResponse.model_rebuild()
UpdateUserRequest.model_rebuild()

__all__ = [
    'GetUserRequest',
    'GetUserResponse',
    'ListUsersRequest',
    'ListUsersResponse',
    'UpdateUserRequest',
]


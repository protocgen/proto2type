// Code generated by proto2type. DO NOT EDIT.
// proto2type v0.8.3
// source: service.proto

package test.v1

import kotlinx.serialization.SerialName
import kotlinx.serialization.Serializable

/** GetUserRequest is the request for GetUser. */
@Serializable
data class GetUserRequest(
    val id: String = ""
)

/** Validates constraints from buf.validate annotations. Returns a list of error messages (empty = valid). */
fun GetUserRequest.validate(): List<String> {
    val errors = mutableListOf<String>()
    return errors
}

/** Validates constraints and throws [IllegalStateException] if any fail. */
fun GetUserRequest.validateOrThrow() {
    val errors = validate()
    if (errors.isNotEmpty()) {
        throw IllegalStateException("GetUserRequest: validation failed: " + errors.joinToString("; "))
    }
}

/**
 * Copies fields from [src] to [dst] based on the given [paths], returning a new instance.
 *
 * Only top-level field names are supported.
 */
fun applyFieldMaskGetUserRequest(dst: GetUserRequest, src: GetUserRequest, paths: List<String>): GetUserRequest {
    var result = dst
    for (path in paths) {
        result = when (path) {
            "id" -> result.copy(id = src.id)
            else -> result
        }
    }
    return result
}

/** GetUserResponse is the response for GetUser. */
@Serializable
data class GetUserResponse(
    val user: User? = null
)

/** Validates constraints from buf.validate annotations. Returns a list of error messages (empty = valid). */
fun GetUserResponse.validate(): List<String> {
    val errors = mutableListOf<String>()
    user?.validate()?.let { errors.addAll(it.map { e -> "user.$e" }) }
    return errors
}

/** Validates constraints and throws [IllegalStateException] if any fail. */
fun GetUserResponse.validateOrThrow() {
    val errors = validate()
    if (errors.isNotEmpty()) {
        throw IllegalStateException("GetUserResponse: validation failed: " + errors.joinToString("; "))
    }
}

/**
 * Copies fields from [src] to [dst] based on the given [paths], returning a new instance.
 *
 * Only top-level field names are supported.
 */
fun applyFieldMaskGetUserResponse(dst: GetUserResponse, src: GetUserResponse, paths: List<String>): GetUserResponse {
    var result = dst
    for (path in paths) {
        result = when (path) {
            "user" -> result.copy(user = src.user)
            else -> result
        }
    }
    return result
}

/** ListUsersRequest is the request for ListUsers. */
@Serializable
data class ListUsersRequest(
    @SerialName("page_size") val pageSize: Int = 0,
    @SerialName("page_token") val pageToken: String = ""
)

/** Validates constraints from buf.validate annotations. Returns a list of error messages (empty = valid). */
fun ListUsersRequest.validate(): List<String> {
    val errors = mutableListOf<String>()
    return errors
}

/** Validates constraints and throws [IllegalStateException] if any fail. */
fun ListUsersRequest.validateOrThrow() {
    val errors = validate()
    if (errors.isNotEmpty()) {
        throw IllegalStateException("ListUsersRequest: validation failed: " + errors.joinToString("; "))
    }
}

/**
 * Copies fields from [src] to [dst] based on the given [paths], returning a new instance.
 *
 * Only top-level field names are supported.
 */
fun applyFieldMaskListUsersRequest(dst: ListUsersRequest, src: ListUsersRequest, paths: List<String>): ListUsersRequest {
    var result = dst
    for (path in paths) {
        result = when (path) {
            "page_size" -> result.copy(pageSize = src.pageSize)
            "page_token" -> result.copy(pageToken = src.pageToken)
            else -> result
        }
    }
    return result
}

/** ListUsersResponse is the response for ListUsers. */
@Serializable
data class ListUsersResponse(
    val users: List<User> = emptyList(),
    @SerialName("next_page_token") val nextPageToken: String = ""
)

/** Validates constraints from buf.validate annotations. Returns a list of error messages (empty = valid). */
fun ListUsersResponse.validate(): List<String> {
    val errors = mutableListOf<String>()
    users.forEachIndexed { i, v -> v.validate().forEach { e -> errors.add("users[$i].$e") } }
    return errors
}

/** Validates constraints and throws [IllegalStateException] if any fail. */
fun ListUsersResponse.validateOrThrow() {
    val errors = validate()
    if (errors.isNotEmpty()) {
        throw IllegalStateException("ListUsersResponse: validation failed: " + errors.joinToString("; "))
    }
}

/**
 * Copies fields from [src] to [dst] based on the given [paths], returning a new instance.
 *
 * Only top-level field names are supported.
 */
fun applyFieldMaskListUsersResponse(dst: ListUsersResponse, src: ListUsersResponse, paths: List<String>): ListUsersResponse {
    var result = dst
    for (path in paths) {
        result = when (path) {
            "users" -> result.copy(users = src.users.toList())
            "next_page_token" -> result.copy(nextPageToken = src.nextPageToken)
            else -> result
        }
    }
    return result
}

/** UpdateUserRequest is the request for UpdateUser. */
@Serializable
data class UpdateUserRequest(
    val user: User? = null,
    @SerialName("update_mask") val updateMask: List<String> = emptyList()
)

/** Validates constraints from buf.validate annotations. Returns a list of error messages (empty = valid). */
fun UpdateUserRequest.validate(): List<String> {
    val errors = mutableListOf<String>()
    user?.validate()?.let { errors.addAll(it.map { e -> "user.$e" }) }
    return errors
}

/** Validates constraints and throws [IllegalStateException] if any fail. */
fun UpdateUserRequest.validateOrThrow() {
    val errors = validate()
    if (errors.isNotEmpty()) {
        throw IllegalStateException("UpdateUserRequest: validation failed: " + errors.joinToString("; "))
    }
}

/**
 * Copies fields from [src] to [dst] based on the given [paths], returning a new instance.
 *
 * Only top-level field names are supported.
 */
fun applyFieldMaskUpdateUserRequest(dst: UpdateUserRequest, src: UpdateUserRequest, paths: List<String>): UpdateUserRequest {
    var result = dst
    for (path in paths) {
        result = when (path) {
            "user" -> result.copy(user = src.user)
            "update_mask" -> result.copy(updateMask = src.updateMask.toList())
            else -> result
        }
    }
    return result
}

/**
 * UserServiceHandler — // UserService provides CRUD operations on Users.
 *
 * All methods use domain types rather than proto types for clean
 * separation between the transport layer and business logic.
 */
interface UserServiceHandler {
    /** GetUser — // GetUser retrieves a user by ID. */
    suspend fun getUser(req: GetUserRequest): GetUserResponse
    /** ListUsers — // ListUsers retrieves a paginated list of users. */
    suspend fun listUsers(req: ListUsersRequest): ListUsersResponse
    /** UpdateUser — // UpdateUser updates a user with field mask. */
    suspend fun updateUser(req: UpdateUserRequest): GetUserResponse
}


// Code generated by proto2type. DO NOT EDIT.
// proto2type v0.3.0
// source: complex.proto
// backend: sqlite

use super::*;
use serde::{Deserialize, Serialize};

#[derive(Debug)]
pub enum ConversionError {
    Json(serde_json::Error),
}

impl std::fmt::Display for ConversionError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Json(e) => write!(f, "json: {e}"),
        }
    }
}

impl std::error::Error for ConversionError {}

impl From<serde_json::Error> for ConversionError {
    fn from(e: serde_json::Error) -> Self {
        Self::Json(e)
    }
}

/// SQLite storage representation of test.v1.Settings.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct SettingsRow {
    pub theme: i32,
    pub locale: String,
}

impl SettingsRow {
    /// Constructs a SettingsRow from a rusqlite::Row using named column access.
    pub fn from_row(row: &rusqlite::Row) -> rusqlite::Result<Self> {
        Ok(Self {
            theme: row.get("theme")?,
            locale: row.get("locale")?,
        })
    }

    /// Converts this row to the domain type.
    pub fn to_domain(&self) -> Result<Settings, ConversionError> {
        Ok(Settings {
            theme: Theme::from_i32(self.theme).unwrap_or_default(),
            locale: self.locale.clone(),
        })
    }

    /// Converts this row into the domain type, consuming self.
    pub fn into_domain(self) -> Result<Settings, ConversionError> {
        Ok(Settings {
            theme: Theme::from_i32(self.theme).unwrap_or_default(),
            locale: self.locale,
        })
    }

    /// Constructs a row from the domain type.
    pub fn from_domain(d: &Settings) -> Result<Self, ConversionError> {
        Ok(Self {
            theme: d.theme as i32,
            locale: d.locale.clone(),
        })
    }
}

impl TryFrom<&Settings> for SettingsRow {
    type Error = ConversionError;
    fn try_from(d: &Settings) -> Result<Self, Self::Error> {
        Self::from_domain(d)
    }
}

impl TryFrom<SettingsRow> for Settings {
    type Error = ConversionError;
    fn try_from(row: SettingsRow) -> Result<Self, Self::Error> {
        row.into_domain()
    }
}

/// SQLite storage representation of test.v1.Organization.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct OrganizationRow {
    pub name: String,
    pub departments: String,
}

impl OrganizationRow {
    /// Constructs a OrganizationRow from a rusqlite::Row using named column access.
    pub fn from_row(row: &rusqlite::Row) -> rusqlite::Result<Self> {
        Ok(Self {
            name: row.get("name")?,
            departments: row.get("departments")?,
        })
    }

    /// Converts this row to the domain type.
    pub fn to_domain(&self) -> Result<Organization, ConversionError> {
        Ok(Organization {
            name: self.name.clone(),
            departments: serde_json::from_str(&self.departments)?,
        })
    }

    /// Converts this row into the domain type, consuming self.
    pub fn into_domain(self) -> Result<Organization, ConversionError> {
        Ok(Organization {
            name: self.name,
            departments: serde_json::from_str(&self.departments)?,
        })
    }

    /// Constructs a row from the domain type.
    pub fn from_domain(d: &Organization) -> Result<Self, ConversionError> {
        Ok(Self {
            name: d.name.clone(),
            departments: serde_json::to_string(&d.departments)?,
        })
    }
}

impl TryFrom<&Organization> for OrganizationRow {
    type Error = ConversionError;
    fn try_from(d: &Organization) -> Result<Self, Self::Error> {
        Self::from_domain(d)
    }
}

impl TryFrom<OrganizationRow> for Organization {
    type Error = ConversionError;
    fn try_from(row: OrganizationRow) -> Result<Self, Self::Error> {
        row.into_domain()
    }
}

/// SQLite storage representation of test.v1.Organization.Department.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct Organization_DepartmentRow {
    pub name: String,
    pub teams: String,
}

impl Organization_DepartmentRow {
    /// Constructs a Organization_DepartmentRow from a rusqlite::Row using named column access.
    pub fn from_row(row: &rusqlite::Row) -> rusqlite::Result<Self> {
        Ok(Self {
            name: row.get("name")?,
            teams: row.get("teams")?,
        })
    }

    /// Converts this row to the domain type.
    pub fn to_domain(&self) -> Result<Organization_Department, ConversionError> {
        Ok(Organization_Department {
            name: self.name.clone(),
            teams: serde_json::from_str(&self.teams)?,
        })
    }

    /// Converts this row into the domain type, consuming self.
    pub fn into_domain(self) -> Result<Organization_Department, ConversionError> {
        Ok(Organization_Department {
            name: self.name,
            teams: serde_json::from_str(&self.teams)?,
        })
    }

    /// Constructs a row from the domain type.
    pub fn from_domain(d: &Organization_Department) -> Result<Self, ConversionError> {
        Ok(Self {
            name: d.name.clone(),
            teams: serde_json::to_string(&d.teams)?,
        })
    }
}

impl TryFrom<&Organization_Department> for Organization_DepartmentRow {
    type Error = ConversionError;
    fn try_from(d: &Organization_Department) -> Result<Self, Self::Error> {
        Self::from_domain(d)
    }
}

impl TryFrom<Organization_DepartmentRow> for Organization_Department {
    type Error = ConversionError;
    fn try_from(row: Organization_DepartmentRow) -> Result<Self, Self::Error> {
        row.into_domain()
    }
}

/// SQLite storage representation of test.v1.Organization.Department.Team.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct Organization_Department_TeamRow {
    pub name: String,
    pub members: String,
}

impl Organization_Department_TeamRow {
    /// Constructs a Organization_Department_TeamRow from a rusqlite::Row using named column access.
    pub fn from_row(row: &rusqlite::Row) -> rusqlite::Result<Self> {
        Ok(Self {
            name: row.get("name")?,
            members: row.get("members")?,
        })
    }

    /// Converts this row to the domain type.
    pub fn to_domain(&self) -> Result<Organization_Department_Team, ConversionError> {
        Ok(Organization_Department_Team {
            name: self.name.clone(),
            members: serde_json::from_str(&self.members)?,
        })
    }

    /// Converts this row into the domain type, consuming self.
    pub fn into_domain(self) -> Result<Organization_Department_Team, ConversionError> {
        Ok(Organization_Department_Team {
            name: self.name,
            members: serde_json::from_str(&self.members)?,
        })
    }

    /// Constructs a row from the domain type.
    pub fn from_domain(d: &Organization_Department_Team) -> Result<Self, ConversionError> {
        Ok(Self {
            name: d.name.clone(),
            members: serde_json::to_string(&d.members)?,
        })
    }
}

impl TryFrom<&Organization_Department_Team> for Organization_Department_TeamRow {
    type Error = ConversionError;
    fn try_from(d: &Organization_Department_Team) -> Result<Self, Self::Error> {
        Self::from_domain(d)
    }
}

impl TryFrom<Organization_Department_TeamRow> for Organization_Department_Team {
    type Error = ConversionError;
    fn try_from(row: Organization_Department_TeamRow) -> Result<Self, Self::Error> {
        row.into_domain()
    }
}

/// SQLite storage representation of test.v1.Notification.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct NotificationRow {
    pub id: String,
    pub channel: Option<String>,
    pub content: Option<String>,
    pub priority: i32,
}

impl NotificationRow {
    /// Constructs a NotificationRow from a rusqlite::Row using named column access.
    pub fn from_row(row: &rusqlite::Row) -> rusqlite::Result<Self> {
        Ok(Self {
            id: row.get("id")?,
            channel: row.get("channel")?,
            content: row.get("content")?,
            priority: row.get("priority")?,
        })
    }

    /// Converts this row to the domain type.
    pub fn to_domain(&self) -> Result<Notification, ConversionError> {
        Ok(Notification {
            id: self.id.clone(),
            channel: match &self.channel { Some(s) => Some(serde_json::from_str(s)?), None => None },
            content: match &self.content { Some(s) => Some(serde_json::from_str(s)?), None => None },
            priority: Priority::from_i32(self.priority).unwrap_or_default(),
        })
    }

    /// Converts this row into the domain type, consuming self.
    pub fn into_domain(self) -> Result<Notification, ConversionError> {
        Ok(Notification {
            id: self.id,
            channel: match self.channel { Some(s) => Some(serde_json::from_str(&s)?), None => None },
            content: match self.content { Some(s) => Some(serde_json::from_str(&s)?), None => None },
            priority: Priority::from_i32(self.priority).unwrap_or_default(),
        })
    }

    /// Constructs a row from the domain type.
    pub fn from_domain(d: &Notification) -> Result<Self, ConversionError> {
        Ok(Self {
            id: d.id.clone(),
            channel: match &d.channel { Some(v) => Some(serde_json::to_string(v)?), None => None },
            content: match &d.content { Some(v) => Some(serde_json::to_string(v)?), None => None },
            priority: d.priority as i32,
        })
    }
}

impl TryFrom<&Notification> for NotificationRow {
    type Error = ConversionError;
    fn try_from(d: &Notification) -> Result<Self, Self::Error> {
        Self::from_domain(d)
    }
}

impl TryFrom<NotificationRow> for Notification {
    type Error = ConversionError;
    fn try_from(row: NotificationRow) -> Result<Self, Self::Error> {
        row.into_domain()
    }
}

/// SQLite storage representation of test.v1.Document.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct DocumentRow {
    pub id: String,
    pub settings_map: String,
    pub code_names: String,
    pub metadata: String,
    pub extension: String,
    pub update_mask: String,
    pub archived: Option<bool>,
    pub view_count: Option<i64>,
    pub placeholders: String,
}

impl DocumentRow {
    /// Constructs a DocumentRow from a rusqlite::Row using named column access.
    pub fn from_row(row: &rusqlite::Row) -> rusqlite::Result<Self> {
        Ok(Self {
            id: row.get("id")?,
            settings_map: row.get("settings_map")?,
            code_names: row.get("code_names")?,
            metadata: row.get("metadata")?,
            extension: row.get("extension")?,
            update_mask: row.get("update_mask")?,
            archived: row.get("archived")?,
            view_count: row.get("view_count")?,
            placeholders: row.get("placeholders")?,
        })
    }

    /// Converts this row to the domain type.
    pub fn to_domain(&self) -> Result<Document, ConversionError> {
        Ok(Document {
            id: self.id.clone(),
            settings_map: serde_json::from_str(&self.settings_map)?,
            code_names: serde_json::from_str(&self.code_names)?,
            metadata: serde_json::from_str(&self.metadata)?,
            extension: serde_json::from_str(&self.extension)?,
            update_mask: serde_json::from_str(&self.update_mask)?,
            archived: self.archived.clone(),
            view_count: self.view_count.clone(),
            placeholders: (),
        })
    }

    /// Converts this row into the domain type, consuming self.
    pub fn into_domain(self) -> Result<Document, ConversionError> {
        Ok(Document {
            id: self.id,
            settings_map: serde_json::from_str(&self.settings_map)?,
            code_names: serde_json::from_str(&self.code_names)?,
            metadata: serde_json::from_str(&self.metadata)?,
            extension: serde_json::from_str(&self.extension)?,
            update_mask: serde_json::from_str(&self.update_mask)?,
            archived: self.archived,
            view_count: self.view_count,
            placeholders: (),
        })
    }

    /// Constructs a row from the domain type.
    pub fn from_domain(d: &Document) -> Result<Self, ConversionError> {
        Ok(Self {
            id: d.id.clone(),
            settings_map: serde_json::to_string(&d.settings_map)?,
            code_names: serde_json::to_string(&d.code_names)?,
            metadata: serde_json::to_string(&d.metadata)?,
            extension: serde_json::to_string(&d.extension)?,
            update_mask: serde_json::to_string(&d.update_mask)?,
            archived: d.archived.clone(),
            view_count: d.view_count.clone(),
            placeholders: "()".to_string(),
        })
    }
}

impl TryFrom<&Document> for DocumentRow {
    type Error = ConversionError;
    fn try_from(d: &Document) -> Result<Self, Self::Error> {
        Self::from_domain(d)
    }
}

impl TryFrom<DocumentRow> for Document {
    type Error = ConversionError;
    fn try_from(row: DocumentRow) -> Result<Self, Self::Error> {
        row.into_domain()
    }
}

/// SQLite storage representation of test.v1.TreeNode.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct TreeNodeRow {
    pub value: String,
    pub children: String,
    pub parent: Option<String>,
}

impl TreeNodeRow {
    /// Constructs a TreeNodeRow from a rusqlite::Row using named column access.
    pub fn from_row(row: &rusqlite::Row) -> rusqlite::Result<Self> {
        Ok(Self {
            value: row.get("value")?,
            children: row.get("children")?,
            parent: row.get("parent")?,
        })
    }

    /// Converts this row to the domain type.
    pub fn to_domain(&self) -> Result<TreeNode, ConversionError> {
        Ok(TreeNode {
            value: self.value.clone(),
            children: serde_json::from_str(&self.children)?,
            parent: match &self.parent { Some(s) => Some(Box::new(serde_json::from_str(s)?)), None => None },
        })
    }

    /// Converts this row into the domain type, consuming self.
    pub fn into_domain(self) -> Result<TreeNode, ConversionError> {
        Ok(TreeNode {
            value: self.value,
            children: serde_json::from_str(&self.children)?,
            parent: match self.parent { Some(s) => Some(Box::new(serde_json::from_str(&s)?)), None => None },
        })
    }

    /// Constructs a row from the domain type.
    pub fn from_domain(d: &TreeNode) -> Result<Self, ConversionError> {
        Ok(Self {
            value: d.value.clone(),
            children: serde_json::to_string(&d.children)?,
            parent: match &d.parent { Some(v) => Some(serde_json::to_string(v.as_ref())?), None => None },
        })
    }
}

impl TryFrom<&TreeNode> for TreeNodeRow {
    type Error = ConversionError;
    fn try_from(d: &TreeNode) -> Result<Self, Self::Error> {
        Self::from_domain(d)
    }
}

impl TryFrom<TreeNodeRow> for TreeNode {
    type Error = ConversionError;
    fn try_from(row: TreeNodeRow) -> Result<Self, Self::Error> {
        row.into_domain()
    }
}

/// SQLite storage representation of test.v1.AuditLog.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct AuditLogRow {
    pub action: String,
    pub user_id: String,
}

impl AuditLogRow {
    /// Constructs a AuditLogRow from a rusqlite::Row using named column access.
    pub fn from_row(row: &rusqlite::Row) -> rusqlite::Result<Self> {
        Ok(Self {
            action: row.get("action")?,
            user_id: row.get("user_id")?,
        })
    }

    /// Converts this row to the domain type.
    pub fn to_domain(&self, id: String) -> Result<AuditLog, ConversionError> {
        Ok(AuditLog {
            id,
            action: self.action.clone(),
            user_id: self.user_id.clone(),
        })
    }

    /// Converts this row into the domain type, consuming self.
    pub fn into_domain(self, id: String) -> Result<AuditLog, ConversionError> {
        Ok(AuditLog {
            id,
            action: self.action,
            user_id: self.user_id,
        })
    }

    /// Constructs a row from the domain type.
    pub fn from_domain(d: &AuditLog) -> Result<Self, ConversionError> {
        Ok(Self {
            action: d.action.clone(),
            user_id: d.user_id.clone(),
        })
    }
}

impl TryFrom<&AuditLog> for AuditLogRow {
    type Error = ConversionError;
    fn try_from(d: &AuditLog) -> Result<Self, Self::Error> {
        Self::from_domain(d)
    }
}


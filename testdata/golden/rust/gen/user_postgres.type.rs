// Code generated by proto2type. DO NOT EDIT.
// proto2type v0.8.3
// source: user.proto
// backend: postgres

use super::*;
use serde::{Deserialize, Serialize};
use chrono::{DateTime, Utc};

#[derive(Debug)]
pub enum ConversionError {
    Json(serde_json::Error),
    InvalidTimestamp(i64),
    InvalidEnumValue(i32),
    Overflow,
}

impl std::fmt::Display for ConversionError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Json(e) => write!(f, "json: {e}"),
            Self::InvalidTimestamp(ms) => write!(f, "invalid timestamp: {ms}ms"),
            Self::InvalidEnumValue(v) => write!(f, "invalid enum value: {v}"),
            Self::Overflow => write!(f, "integer overflow during conversion"),
        }
    }
}

impl std::error::Error for ConversionError {}

impl From<serde_json::Error> for ConversionError {
    fn from(e: serde_json::Error) -> Self {
        Self::Json(e)
    }
}

/// Converts epoch milliseconds to a chrono DateTime<Utc>.
#[allow(dead_code)]
fn epoch_ms_to_datetime(ms: i64) -> Result<DateTime<Utc>, ConversionError> {
    DateTime::from_timestamp_millis(ms).ok_or(ConversionError::InvalidTimestamp(ms))
}

/// Converts a chrono DateTime<Utc> to epoch milliseconds.
#[allow(dead_code)]
fn datetime_to_epoch_ms(dt: &DateTime<Utc>) -> i64 {
    dt.timestamp_millis()
}

/// PostgreSQL storage representation of test.v1.User.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, sqlx::FromRow)]
pub struct UserPostgres {
    pub id: String,
    pub email: String,
    pub display_name: String,
    pub active: bool,
    pub age: i32,
    pub roles: sqlx::types::Json<Vec<String>>,
    pub metadata: sqlx::types::Json<std::collections::HashMap<String, String>>,
    pub address: Option<sqlx::types::Json<AddressPostgres>>,
    pub created_at: DateTime<Utc>,
    pub session_timeout: i64,
    pub phone: Option<String>,
    pub avatar: Vec<u8>,
    pub nickname: Option<String>,
    pub status: i32,
    pub contact_method: Option<sqlx::types::Json<serde_json::Value>>,
    pub tags: sqlx::types::Json<Vec<Tag>>,
    pub deleted_at: Option<DateTime<Utc>>,
    pub previous_status: Option<i32>,
    pub update_mask: Vec<String>,
    pub extra_metadata: sqlx::types::Json<serde_json::Value>,
    pub preferences: sqlx::types::Json<serde_json::Value>,
    pub avatar_thumbnail: Option<Vec<u8>>,
    pub field_masks: sqlx::types::Json<Vec<String>>,
    pub structs: sqlx::types::Json<Vec<String>>,
    pub lists: sqlx::types::Json<Vec<String>>,
    pub event_times: sqlx::types::Json<std::collections::HashMap<String, serde_json::Value>>,
    pub configs: sqlx::types::Json<std::collections::HashMap<String, serde_json::Value>>,
    pub single_value: sqlx::types::Json<serde_json::Value>,
    pub values: sqlx::types::Json<Vec<String>>,
    pub value_map: sqlx::types::Json<std::collections::HashMap<String, serde_json::Value>>,
    pub labels: sqlx::types::Json<std::collections::HashMap<String, serde_json::Value>>,
    pub scores: sqlx::types::Json<std::collections::HashMap<String, serde_json::Value>>,
    pub old_field: String,
    pub optional_name: String,
    pub big_number: i64,
    pub handle: String,
}

impl UserPostgres {
    /// Converts this row to the domain type.
    pub fn to_domain(&self) -> Result<User, ConversionError> {
        Ok(User {
            id: self.id.clone(),
            email: self.email.clone(),
            display_name: self.display_name.clone(),
            active: self.active.clone(),
            age: self.age.clone(),
            roles: self.roles.0.clone(),
            metadata: self.metadata.0.clone(),
            address: self.address.as_ref().map(|j| j.0.clone()),
            created_at: self.created_at.clone(),
            session_timeout: self.session_timeout,
            phone: self.phone.clone(),
            avatar: self.avatar.clone(),
            nickname: self.nickname.clone(),
            status: UserStatus::try_from(self.status)?,
            contact_method: match &self.contact_method { Some(json) => Some(serde_json::from_value(json.0.clone())?), None => None },
            tags: self.tags.0.clone(),
            deleted_at: self.deleted_at.clone(),
            previous_status: self.previous_status.map(|v| UserStatus::try_from(v)).transpose()?,
            update_mask: serde_json::from_value(self.update_mask.0.clone())?,
            extra_metadata: serde_json::from_value(self.extra_metadata.0.clone())?,
            preferences: serde_json::from_value(self.preferences.0.clone())?,
            avatar_thumbnail: self.avatar_thumbnail.clone(),
            field_masks: self.field_masks.0.clone(),
            structs: self.structs.0.clone(),
            lists: self.lists.0.clone(),
            event_times: self.event_times.0.clone(),
            configs: self.configs.0.clone(),
            single_value: serde_json::from_value(self.single_value.0.clone())?,
            values: self.values.0.clone(),
            value_map: self.value_map.0.clone(),
            labels: self.labels.0.clone(),
            scores: self.scores.0.clone(),
            old_field: self.old_field.clone(),
            optional_name: self.optional_name.clone(),
            big_number: self.big_number.clone(),
            handle: self.handle.clone(),
        })
    }

    /// Converts this row into the domain type, consuming self.
    pub fn into_domain(self) -> Result<User, ConversionError> {
        Ok(User {
            id: self.id,
            email: self.email,
            display_name: self.display_name,
            active: self.active,
            age: self.age,
            roles: self.roles.0,
            metadata: self.metadata.0,
            address: self.address.map(|j| j.0),
            created_at: self.created_at.map(|j| j.0),
            session_timeout: self.session_timeout.map(|j| j.0),
            phone: self.phone,
            avatar: self.avatar,
            nickname: self.nickname.map(|j| j.0),
            status: UserStatus::try_from(self.status)?,
            contact_method: match self.contact_method { Some(json) => Some(serde_json::from_value(json.0)?), None => None },
            tags: self.tags.0,
            deleted_at: self.deleted_at.map(|j| j.0),
            previous_status: self.previous_status.map(|v| UserStatus::try_from(v)).transpose()?,
            update_mask: self.update_mask.map(|j| j.0),
            extra_metadata: self.extra_metadata.map(|j| j.0),
            preferences: self.preferences.map(|j| j.0),
            avatar_thumbnail: self.avatar_thumbnail,
            field_masks: self.field_masks.0,
            structs: self.structs.0,
            lists: self.lists.0,
            event_times: self.event_times.0,
            configs: self.configs.0,
            single_value: self.single_value.map(|j| j.0),
            values: self.values.0,
            value_map: self.value_map.0,
            labels: self.labels.0,
            scores: self.scores.0,
            old_field: self.old_field,
            optional_name: self.optional_name,
            big_number: self.big_number,
            handle: self.handle,
        })
    }

    /// Constructs a row from the domain type.
    pub fn from_domain(d: &User) -> Result<Self, ConversionError> {
        Ok(Self {
            id: d.id.clone(),
            email: d.email.clone(),
            display_name: d.display_name.clone(),
            active: d.active.clone(),
            age: d.age.clone(),
            roles: sqlx::types::Json(d.roles.clone()),
            metadata: sqlx::types::Json(d.metadata.clone()),
            address: d.address.as_ref().map(|v| sqlx::types::Json(v.clone())),
            created_at: d.created_at.clone(),
            session_timeout: d.session_timeout,
            phone: d.phone.clone(),
            avatar: d.avatar.clone(),
            nickname: d.nickname.clone(),
            status: d.status as i32,
            contact_method: match &d.contact_method { Some(v) => Some(sqlx::types::Json(serde_json::to_value(v)?)), None => None },
            tags: sqlx::types::Json(d.tags.clone()),
            deleted_at: d.deleted_at.clone(),
            previous_status: d.previous_status.map(|e| e as i32),
            update_mask: sqlx::types::Json(serde_json::to_value(&d.update_mask)?),
            extra_metadata: sqlx::types::Json(serde_json::to_value(&d.extra_metadata)?),
            preferences: sqlx::types::Json(serde_json::to_value(&d.preferences)?),
            avatar_thumbnail: d.avatar_thumbnail.clone(),
            field_masks: sqlx::types::Json(d.field_masks.clone()),
            structs: sqlx::types::Json(d.structs.clone()),
            lists: sqlx::types::Json(d.lists.clone()),
            event_times: sqlx::types::Json(d.event_times.clone()),
            configs: sqlx::types::Json(d.configs.clone()),
            single_value: sqlx::types::Json(serde_json::to_value(&d.single_value)?),
            values: sqlx::types::Json(d.values.clone()),
            value_map: sqlx::types::Json(d.value_map.clone()),
            labels: sqlx::types::Json(d.labels.clone()),
            scores: sqlx::types::Json(d.scores.clone()),
            old_field: d.old_field.clone(),
            optional_name: d.optional_name.clone(),
            big_number: d.big_number.clone(),
            handle: d.handle.clone(),
        })
    }
}

impl TryFrom<&User> for UserPostgres {
    type Error = ConversionError;
    fn try_from(d: &User) -> Result<Self, Self::Error> {
        Self::from_domain(d)
    }
}

impl TryFrom<UserPostgres> for User {
    type Error = ConversionError;
    fn try_from(row: UserPostgres) -> Result<Self, Self::Error> {
        row.into_domain()
    }
}

/// PostgreSQL storage representation of test.v1.Address.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, sqlx::FromRow)]
pub struct AddressPostgres {
    pub street: String,
    pub city: String,
    pub state: String,
    pub zip: String,
    pub country: String,
}

impl AddressPostgres {
    /// Converts this row to the domain type.
    pub fn to_domain(&self) -> Result<Address, ConversionError> {
        Ok(Address {
            street: self.street.clone(),
            city: self.city.clone(),
            state: self.state.clone(),
            zip: self.zip.clone(),
            country: self.country.clone(),
        })
    }

    /// Converts this row into the domain type, consuming self.
    pub fn into_domain(self) -> Result<Address, ConversionError> {
        Ok(Address {
            street: self.street,
            city: self.city,
            state: self.state,
            zip: self.zip,
            country: self.country,
        })
    }

    /// Constructs a row from the domain type.
    pub fn from_domain(d: &Address) -> Result<Self, ConversionError> {
        Ok(Self {
            street: d.street.clone(),
            city: d.city.clone(),
            state: d.state.clone(),
            zip: d.zip.clone(),
            country: d.country.clone(),
        })
    }
}

impl TryFrom<&Address> for AddressPostgres {
    type Error = ConversionError;
    fn try_from(d: &Address) -> Result<Self, Self::Error> {
        Self::from_domain(d)
    }
}

impl TryFrom<AddressPostgres> for Address {
    type Error = ConversionError;
    fn try_from(row: AddressPostgres) -> Result<Self, Self::Error> {
        row.into_domain()
    }
}

/// PostgreSQL storage representation of test.v1.Tag.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, sqlx::FromRow)]
pub struct TagPostgres {
    pub key: String,
    pub value: String,
}

impl TagPostgres {
    /// Converts this row to the domain type.
    pub fn to_domain(&self) -> Result<Tag, ConversionError> {
        Ok(Tag {
            key: self.key.clone(),
            value: self.value.clone(),
        })
    }

    /// Converts this row into the domain type, consuming self.
    pub fn into_domain(self) -> Result<Tag, ConversionError> {
        Ok(Tag {
            key: self.key,
            value: self.value,
        })
    }

    /// Constructs a row from the domain type.
    pub fn from_domain(d: &Tag) -> Result<Self, ConversionError> {
        Ok(Self {
            key: d.key.clone(),
            value: d.value.clone(),
        })
    }
}

impl TryFrom<&Tag> for TagPostgres {
    type Error = ConversionError;
    fn try_from(d: &Tag) -> Result<Self, Self::Error> {
        Self::from_domain(d)
    }
}

impl TryFrom<TagPostgres> for Tag {
    type Error = ConversionError;
    fn try_from(row: TagPostgres) -> Result<Self, Self::Error> {
        row.into_domain()
    }
}

/// PostgreSQL storage representation of test.v1.Category.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, sqlx::FromRow)]
pub struct CategoryPostgres {
    pub name: String,
    pub parent: Option<Box<CategoryPostgres>>,
    pub children: sqlx::types::Json<Vec<Category>>,
}

impl CategoryPostgres {
    /// Converts this row to the domain type.
    pub fn to_domain(&self) -> Result<Category, ConversionError> {
        Ok(Category {
            name: self.name.clone(),
            parent: self.parent.as_ref().map(|b| *b.clone()),
            children: self.children.0.clone(),
        })
    }

    /// Converts this row into the domain type, consuming self.
    pub fn into_domain(self) -> Result<Category, ConversionError> {
        Ok(Category {
            name: self.name,
            parent: self.parent.map(|b| *b),
            children: self.children.0,
        })
    }

    /// Constructs a row from the domain type.
    pub fn from_domain(d: &Category) -> Result<Self, ConversionError> {
        Ok(Self {
            name: d.name.clone(),
            parent: d.parent.as_ref().map(|v| Box::new((**v).clone())),
            children: sqlx::types::Json(d.children.clone()),
        })
    }
}

impl TryFrom<&Category> for CategoryPostgres {
    type Error = ConversionError;
    fn try_from(d: &Category) -> Result<Self, Self::Error> {
        Self::from_domain(d)
    }
}

impl TryFrom<CategoryPostgres> for Category {
    type Error = ConversionError;
    fn try_from(row: CategoryPostgres) -> Result<Self, Self::Error> {
        row.into_domain()
    }
}


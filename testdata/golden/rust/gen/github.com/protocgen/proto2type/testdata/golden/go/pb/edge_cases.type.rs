// Code generated by proto2type. DO NOT EDIT.
// proto2type v0.8.3
// source: edge_cases.proto

use serde::{Deserialize, Serialize};
use chrono::{DateTime, Utc};
use std::collections::HashMap;

/// SingleValueEnum has only the UNSPECIFIED sentinel — tests that validators,
/// defined_only checks, and type unions don't break on single-variant enums.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Default, Serialize, Deserialize)]
#[repr(i32)]
pub enum SingleValueEnum {
    #[default]
    Unspecified = 0,
}

impl SingleValueEnum {
    pub fn from_i32(value: i32) -> Option<Self> {
        match value {
            0 => Some(Self::Unspecified),
            _ => None,
        }
    }
}

impl std::fmt::Display for SingleValueEnum {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Unspecified => write!(f, "SINGLE_VALUE_ENUM_UNSPECIFIED"),
        }
    }
}

/// Domain representation of test.v1.EmptyMessage.
///
/// EmptyMessage has zero fields — tests that code generation produces valid
/// types, Clone, Equal, ToProto, FromProto, and Validate without panicking.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct EmptyMessage {
}

impl EmptyMessage {
    /// Copies fields from `src` to `self` based on the given paths.
    ///
    /// Only top-level field names are supported.
    pub fn apply_field_mask(&mut self, src: &Self, paths: &[&str]) {
        for path in paths {
            match *path {
                _ => {} // ignore unknown paths
            }
        }
    }
}

/// Domain representation of test.v1.AllOptionalScalars.
///
/// AllOptionalScalars has every field marked optional — tests pointer-heavy
/// struct generation, all-nil instantiation, Clone independence, and JSON
/// omitempty for every proto3 optional scalar type.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct AllOptionalScalars {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub opt_string: Option<String>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub opt_int32: Option<i32>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub opt_int64: Option<i64>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub opt_bool: Option<bool>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub opt_double: Option<f64>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub opt_float: Option<f32>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub opt_bytes: Option<Vec<u8>>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub opt_timestamp: Option<DateTime<Utc>>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub opt_enum: Option<SingleValueEnum>,
}

impl AllOptionalScalars {
    /// Copies fields from `src` to `self` based on the given paths.
    ///
    /// Only top-level field names are supported.
    pub fn apply_field_mask(&mut self, src: &Self, paths: &[&str]) {
        for path in paths {
            match *path {
                "opt_string" => self.opt_string = src.opt_string.clone(),
                "opt_int32" => self.opt_int32 = src.opt_int32.clone(),
                "opt_int64" => self.opt_int64 = src.opt_int64.clone(),
                "opt_bool" => self.opt_bool = src.opt_bool.clone(),
                "opt_double" => self.opt_double = src.opt_double.clone(),
                "opt_float" => self.opt_float = src.opt_float.clone(),
                "opt_bytes" => self.opt_bytes = src.opt_bytes.clone(),
                "opt_timestamp" => self.opt_timestamp = src.opt_timestamp.clone(),
                "opt_enum" => self.opt_enum = src.opt_enum.clone(),
                _ => {} // ignore unknown paths
            }
        }
    }
}

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(tag = "kind", content = "value")]
pub enum OneofOnlyPayload {
    #[serde(rename = "text")]
    Text(String),
    #[serde(rename = "number")]
    Number(i32),
    #[serde(rename = "flag")]
    Flag(bool),
}

/// Domain representation of test.v1.OneofOnly.
///
/// OneofOnly has zero regular fields and a single oneof — tests that
/// generators don't assume at least one standard field exists.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct OneofOnly {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub payload: Option<OneofOnlyPayload>,
}

impl OneofOnly {
    /// Copies fields from `src` to `self` based on the given paths.
    ///
    /// Only top-level field names are supported.
    pub fn apply_field_mask(&mut self, src: &Self, paths: &[&str]) {
        for path in paths {
            match *path {
                "text" => self.payload = src.payload.clone(),
                "number" => self.payload = src.payload.clone(),
                "flag" => self.payload = src.payload.clone(),
                _ => {} // ignore unknown paths
            }
        }
    }
}

/// Domain representation of test.v1.MapOnly.
///
/// MapOnly contains only map fields — tests nil-map vs empty-map semantics,
/// map cloning, and equality without any scalar anchors.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct MapOnly {
    #[serde(default)]
    pub string_map: HashMap<String, String>,
    #[serde(default)]
    pub int_map: HashMap<i32, String>,
}

impl MapOnly {
    /// Copies fields from `src` to `self` based on the given paths.
    ///
    /// Only top-level field names are supported.
    pub fn apply_field_mask(&mut self, src: &Self, paths: &[&str]) {
        for path in paths {
            match *path {
                "string_map" => self.string_map = src.string_map.clone(),
                "int_map" => self.int_map = src.int_map.clone(),
                _ => {} // ignore unknown paths
            }
        }
    }
}

/// Domain representation of test.v1.RepeatedOnly.
///
/// RepeatedOnly contains only repeated fields — tests nil-slice vs
/// empty-slice semantics, slice cloning, and equality.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize)]
#[non_exhaustive]
pub struct RepeatedOnly {
    #[serde(default)]
    pub items: Vec<String>,
    #[serde(default)]
    pub numbers: Vec<i32>,
}

impl RepeatedOnly {
    /// Copies fields from `src` to `self` based on the given paths.
    ///
    /// Only top-level field names are supported.
    pub fn apply_field_mask(&mut self, src: &Self, paths: &[&str]) {
        for path in paths {
            match *path {
                "items" => self.items = src.items.clone(),
                "numbers" => self.numbers = src.numbers.clone(),
                _ => {} // ignore unknown paths
            }
        }
    }
}


// Code generated by proto2type. DO NOT EDIT.
// proto2type v0.8.3
// source: service.proto
// backend: buffa

use super::*;

use crate::proto as __buffa_mod;

#[derive(Debug)]
pub enum ConversionError {
    InvalidEnumValue(i32),
    MissingRequiredField(&'static str),
}

impl std::fmt::Display for ConversionError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::InvalidEnumValue(v) => write!(f, "invalid enum value: {v}"),
            Self::MissingRequiredField(field) => write!(f, "missing required field: {field}"),
        }
    }
}

impl std::error::Error for ConversionError {}

#[allow(clippy::field_reassign_with_default)]
impl TryFrom<&GetUserRequest> for __buffa_mod::GetUserRequest {
    type Error = ConversionError;

    fn try_from(d: &GetUserRequest) -> Result<Self, Self::Error> {
        let mut b = Self::default();
        b.id = d.id.clone().into();
        Ok(b)
    }
}

#[allow(clippy::field_reassign_with_default)]
impl TryFrom<&__buffa_mod::GetUserRequest> for GetUserRequest {
    type Error = ConversionError;

    fn try_from(b: &__buffa_mod::GetUserRequest) -> Result<Self, Self::Error> {
        let mut d = Self::default();
        d.id = b.id.to_string();
        Ok(d)
    }
}

#[allow(clippy::field_reassign_with_default)]
impl TryFrom<&GetUserResponse> for __buffa_mod::GetUserResponse {
    type Error = ConversionError;

    fn try_from(d: &GetUserResponse) -> Result<Self, Self::Error> {
        let mut b = Self::default();
        b.user = buffa::MessageField::some((&d.user).try_into()?);
        Ok(b)
    }
}

#[allow(clippy::field_reassign_with_default)]
impl TryFrom<&__buffa_mod::GetUserResponse> for GetUserResponse {
    type Error = ConversionError;

    fn try_from(b: &__buffa_mod::GetUserResponse) -> Result<Self, Self::Error> {
        let mut d = Self::default();
        d.user = b.user.as_option().ok_or(ConversionError::MissingRequiredField("user"))?.try_into()?;
        Ok(d)
    }
}

#[allow(clippy::field_reassign_with_default)]
impl TryFrom<&ListUsersRequest> for __buffa_mod::ListUsersRequest {
    type Error = ConversionError;

    fn try_from(d: &ListUsersRequest) -> Result<Self, Self::Error> {
        let mut b = Self::default();
        b.page_size = d.page_size;
        b.page_token = d.page_token.clone().into();
        Ok(b)
    }
}

#[allow(clippy::field_reassign_with_default)]
impl TryFrom<&__buffa_mod::ListUsersRequest> for ListUsersRequest {
    type Error = ConversionError;

    fn try_from(b: &__buffa_mod::ListUsersRequest) -> Result<Self, Self::Error> {
        let mut d = Self::default();
        d.page_size = b.page_size;
        d.page_token = b.page_token.to_string();
        Ok(d)
    }
}

#[allow(clippy::field_reassign_with_default)]
impl TryFrom<&ListUsersResponse> for __buffa_mod::ListUsersResponse {
    type Error = ConversionError;

    fn try_from(d: &ListUsersResponse) -> Result<Self, Self::Error> {
        let mut b = Self::default();
        b.users = d.users.iter().map(|v| v.try_into()).collect::<Result<Vec<_>, _>>()?;
        b.next_page_token = d.next_page_token.clone().into();
        Ok(b)
    }
}

#[allow(clippy::field_reassign_with_default)]
impl TryFrom<&__buffa_mod::ListUsersResponse> for ListUsersResponse {
    type Error = ConversionError;

    fn try_from(b: &__buffa_mod::ListUsersResponse) -> Result<Self, Self::Error> {
        let mut d = Self::default();
        d.users = b.users.iter().map(|v| v.try_into()).collect::<Result<Vec<_>, _>>()?;
        d.next_page_token = b.next_page_token.to_string();
        Ok(d)
    }
}

#[allow(clippy::field_reassign_with_default)]
impl TryFrom<&UpdateUserRequest> for __buffa_mod::UpdateUserRequest {
    type Error = ConversionError;

    fn try_from(d: &UpdateUserRequest) -> Result<Self, Self::Error> {
        let mut b = Self::default();
        b.user = buffa::MessageField::some((&d.user).try_into()?);
        b.update_mask = d.update_mask.iter().map(|s| s.clone().into()).collect();
        Ok(b)
    }
}

#[allow(clippy::field_reassign_with_default)]
impl TryFrom<&__buffa_mod::UpdateUserRequest> for UpdateUserRequest {
    type Error = ConversionError;

    fn try_from(b: &__buffa_mod::UpdateUserRequest) -> Result<Self, Self::Error> {
        let mut d = Self::default();
        d.user = b.user.as_option().ok_or(ConversionError::MissingRequiredField("user"))?.try_into()?;
        d.update_mask = b.update_mask.iter().map(|s| s.to_string()).collect();
        Ok(d)
    }
}


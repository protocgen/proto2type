// Code generated by proto2type. DO NOT EDIT.
// proto2type v0.8.3
// source: service.proto

use serde::{Deserialize, Serialize};
use validator::{Validate, ValidateEmail};

/// Domain representation of test.v1.GetUserRequest.
///
/// GetUserRequest is the request for GetUser.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize, Validate)]
#[non_exhaustive]
pub struct GetUserRequest {
    pub id: String,
}

impl GetUserRequest {
    /// Copies fields from `src` to `self` based on the given paths.
    ///
    /// Only top-level field names are supported.
    pub fn apply_field_mask(&mut self, src: &Self, paths: &[&str]) {
        for path in paths {
            match *path {
                "id" => self.id = src.id.clone(),
                _ => {} // ignore unknown paths
            }
        }
    }
}

/// Domain representation of test.v1.GetUserResponse.
///
/// GetUserResponse is the response for GetUser.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize, Validate)]
#[non_exhaustive]
pub struct GetUserResponse {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[validate(nested)]
    pub user: Option<User>,
}

impl GetUserResponse {
    /// Copies fields from `src` to `self` based on the given paths.
    ///
    /// Only top-level field names are supported.
    pub fn apply_field_mask(&mut self, src: &Self, paths: &[&str]) {
        for path in paths {
            match *path {
                "user" => self.user = src.user.clone(),
                _ => {} // ignore unknown paths
            }
        }
    }
}

/// Domain representation of test.v1.ListUsersRequest.
///
/// ListUsersRequest is the request for ListUsers.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize, Validate)]
#[non_exhaustive]
pub struct ListUsersRequest {
    pub page_size: i32,
    pub page_token: String,
}

impl ListUsersRequest {
    /// Copies fields from `src` to `self` based on the given paths.
    ///
    /// Only top-level field names are supported.
    pub fn apply_field_mask(&mut self, src: &Self, paths: &[&str]) {
        for path in paths {
            match *path {
                "page_size" => self.page_size = src.page_size.clone(),
                "page_token" => self.page_token = src.page_token.clone(),
                _ => {} // ignore unknown paths
            }
        }
    }
}

/// Domain representation of test.v1.ListUsersResponse.
///
/// ListUsersResponse is the response for ListUsers.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize, Validate)]
#[non_exhaustive]
pub struct ListUsersResponse {
    #[serde(default)]
    #[validate(nested)]
    pub users: Vec<User>,
    pub next_page_token: String,
}

impl ListUsersResponse {
    /// Copies fields from `src` to `self` based on the given paths.
    ///
    /// Only top-level field names are supported.
    pub fn apply_field_mask(&mut self, src: &Self, paths: &[&str]) {
        for path in paths {
            match *path {
                "users" => self.users = src.users.clone(),
                "next_page_token" => self.next_page_token = src.next_page_token.clone(),
                _ => {} // ignore unknown paths
            }
        }
    }
}

/// Domain representation of test.v1.UpdateUserRequest.
///
/// UpdateUserRequest is the request for UpdateUser.
#[derive(Debug, Clone, Default, PartialEq, Serialize, Deserialize, Validate)]
#[non_exhaustive]
pub struct UpdateUserRequest {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    #[validate(nested)]
    pub user: Option<User>,
    #[serde(default)]
    pub update_mask: Vec<String>,
}

impl UpdateUserRequest {
    /// Copies fields from `src` to `self` based on the given paths.
    ///
    /// Only top-level field names are supported.
    pub fn apply_field_mask(&mut self, src: &Self, paths: &[&str]) {
        for path in paths {
            match *path {
                "user" => self.user = src.user.clone(),
                "update_mask" => self.update_mask = src.update_mask.clone(),
                _ => {} // ignore unknown paths
            }
        }
    }
}

/// UserServiceHandler — UserService provides CRUD operations on Users.
///
/// All methods use domain types rather than proto types for clean
/// separation between the transport layer and business logic.
#[async_trait::async_trait]
pub trait UserServiceHandler {
    /// GetUser retrieves a user by ID.
    async fn get_user(&self, req: GetUserRequest) -> Result<GetUserResponse, Box<dyn std::error::Error>>;
    /// ListUsers retrieves a paginated list of users.
    async fn list_users(&self, req: ListUsersRequest) -> Result<ListUsersResponse, Box<dyn std::error::Error>>;
    /// UpdateUser updates a user with field mask.
    async fn update_user(&self, req: UpdateUserRequest) -> Result<GetUserResponse, Box<dyn std::error::Error>>;
}


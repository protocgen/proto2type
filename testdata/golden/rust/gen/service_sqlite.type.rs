// Code generated by proto2type. DO NOT EDIT.
// proto2type v0.8.3
// source: service.proto
// backend: sqlite

use super::*;
use serde::{Deserialize, Serialize};

#[derive(Debug)]
pub enum ConversionError {
    Json(serde_json::Error),
    InvalidEnumValue(i32),
    Overflow,
}

impl std::fmt::Display for ConversionError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Json(e) => write!(f, "json: {e}"),
            Self::InvalidEnumValue(v) => write!(f, "invalid enum value: {v}"),
            Self::Overflow => write!(f, "integer overflow during conversion"),
        }
    }
}

impl std::error::Error for ConversionError {}

impl From<serde_json::Error> for ConversionError {
    fn from(e: serde_json::Error) -> Self {
        Self::Json(e)
    }
}

/// SQLite storage representation of test.v1.GetUserRequest.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct GetUserRequestRow {
    pub id: String,
}

impl GetUserRequestRow {
    /// Constructs a GetUserRequestRow from a rusqlite::Row using named column access.
    pub fn from_row(row: &rusqlite::Row) -> rusqlite::Result<Self> {
        Ok(Self {
            id: row.get::<_, Option<String>>("id")?.unwrap_or_default(),
        })
    }

    /// Converts this row to the domain type.
    pub fn to_domain(&self) -> Result<GetUserRequest, ConversionError> {
        Ok(GetUserRequest {
            id: self.id.clone(),
        })
    }

    /// Converts this row into the domain type, consuming self.
    pub fn into_domain(self) -> Result<GetUserRequest, ConversionError> {
        Ok(GetUserRequest {
            id: self.id,
        })
    }

    /// Constructs a row from the domain type.
    pub fn from_domain(d: &GetUserRequest) -> Result<Self, ConversionError> {
        Ok(Self {
            id: d.id.clone(),
        })
    }
}

impl TryFrom<&GetUserRequest> for GetUserRequestRow {
    type Error = ConversionError;
    fn try_from(d: &GetUserRequest) -> Result<Self, Self::Error> {
        Self::from_domain(d)
    }
}

impl TryFrom<GetUserRequestRow> for GetUserRequest {
    type Error = ConversionError;
    fn try_from(row: GetUserRequestRow) -> Result<Self, Self::Error> {
        row.into_domain()
    }
}

/// SQLite storage representation of test.v1.GetUserResponse.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct GetUserResponseRow {
    pub user: Option<String>,
}

impl GetUserResponseRow {
    /// Constructs a GetUserResponseRow from a rusqlite::Row using named column access.
    pub fn from_row(row: &rusqlite::Row) -> rusqlite::Result<Self> {
        Ok(Self {
            user: row.get::<_, Option<String>>("user")?,
        })
    }

    /// Converts this row to the domain type.
    pub fn to_domain(&self) -> Result<GetUserResponse, ConversionError> {
        Ok(GetUserResponse {
            user: match &self.user { Some(s) => Some(serde_json::from_str(s)?), None => None },
        })
    }

    /// Converts this row into the domain type, consuming self.
    pub fn into_domain(self) -> Result<GetUserResponse, ConversionError> {
        Ok(GetUserResponse {
            user: match self.user { Some(s) => Some(serde_json::from_str(&s)?), None => None },
        })
    }

    /// Constructs a row from the domain type.
    pub fn from_domain(d: &GetUserResponse) -> Result<Self, ConversionError> {
        Ok(Self {
            user: match &d.user { Some(v) => Some(serde_json::to_string(v)?), None => None },
        })
    }
}

impl TryFrom<&GetUserResponse> for GetUserResponseRow {
    type Error = ConversionError;
    fn try_from(d: &GetUserResponse) -> Result<Self, Self::Error> {
        Self::from_domain(d)
    }
}

impl TryFrom<GetUserResponseRow> for GetUserResponse {
    type Error = ConversionError;
    fn try_from(row: GetUserResponseRow) -> Result<Self, Self::Error> {
        row.into_domain()
    }
}

/// SQLite storage representation of test.v1.ListUsersRequest.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct ListUsersRequestRow {
    pub page_size: i32,
    pub page_token: String,
}

impl ListUsersRequestRow {
    /// Constructs a ListUsersRequestRow from a rusqlite::Row using named column access.
    pub fn from_row(row: &rusqlite::Row) -> rusqlite::Result<Self> {
        Ok(Self {
            page_size: row.get::<_, Option<i32>>("page_size")?.unwrap_or_default(),
            page_token: row.get::<_, Option<String>>("page_token")?.unwrap_or_default(),
        })
    }

    /// Converts this row to the domain type.
    pub fn to_domain(&self) -> Result<ListUsersRequest, ConversionError> {
        Ok(ListUsersRequest {
            page_size: self.page_size,
            page_token: self.page_token.clone(),
        })
    }

    /// Converts this row into the domain type, consuming self.
    pub fn into_domain(self) -> Result<ListUsersRequest, ConversionError> {
        Ok(ListUsersRequest {
            page_size: self.page_size,
            page_token: self.page_token,
        })
    }

    /// Constructs a row from the domain type.
    pub fn from_domain(d: &ListUsersRequest) -> Result<Self, ConversionError> {
        Ok(Self {
            page_size: d.page_size,
            page_token: d.page_token.clone(),
        })
    }
}

impl TryFrom<&ListUsersRequest> for ListUsersRequestRow {
    type Error = ConversionError;
    fn try_from(d: &ListUsersRequest) -> Result<Self, Self::Error> {
        Self::from_domain(d)
    }
}

impl TryFrom<ListUsersRequestRow> for ListUsersRequest {
    type Error = ConversionError;
    fn try_from(row: ListUsersRequestRow) -> Result<Self, Self::Error> {
        row.into_domain()
    }
}

/// SQLite storage representation of test.v1.ListUsersResponse.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct ListUsersResponseRow {
    pub users: Option<String>,
    pub next_page_token: String,
}

impl ListUsersResponseRow {
    /// Constructs a ListUsersResponseRow from a rusqlite::Row using named column access.
    pub fn from_row(row: &rusqlite::Row) -> rusqlite::Result<Self> {
        Ok(Self {
            users: row.get::<_, Option<String>>("users")?,
            next_page_token: row.get::<_, Option<String>>("next_page_token")?.unwrap_or_default(),
        })
    }

    /// Converts this row to the domain type.
    pub fn to_domain(&self) -> Result<ListUsersResponse, ConversionError> {
        Ok(ListUsersResponse {
            users: serde_json::from_str(&self.users.as_deref().unwrap_or("[]"))?,
            next_page_token: self.next_page_token.clone(),
        })
    }

    /// Converts this row into the domain type, consuming self.
    pub fn into_domain(self) -> Result<ListUsersResponse, ConversionError> {
        Ok(ListUsersResponse {
            users: serde_json::from_str(&self.users.as_deref().unwrap_or("[]"))?,
            next_page_token: self.next_page_token,
        })
    }

    /// Constructs a row from the domain type.
    pub fn from_domain(d: &ListUsersResponse) -> Result<Self, ConversionError> {
        Ok(Self {
            users: Some(serde_json::to_string(&d.users)?),
            next_page_token: d.next_page_token.clone(),
        })
    }
}

impl TryFrom<&ListUsersResponse> for ListUsersResponseRow {
    type Error = ConversionError;
    fn try_from(d: &ListUsersResponse) -> Result<Self, Self::Error> {
        Self::from_domain(d)
    }
}

impl TryFrom<ListUsersResponseRow> for ListUsersResponse {
    type Error = ConversionError;
    fn try_from(row: ListUsersResponseRow) -> Result<Self, Self::Error> {
        row.into_domain()
    }
}

/// SQLite storage representation of test.v1.UpdateUserRequest.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct UpdateUserRequestRow {
    pub user: Option<String>,
    pub update_mask: Option<String>,
}

impl UpdateUserRequestRow {
    /// Constructs a UpdateUserRequestRow from a rusqlite::Row using named column access.
    pub fn from_row(row: &rusqlite::Row) -> rusqlite::Result<Self> {
        Ok(Self {
            user: row.get::<_, Option<String>>("user")?,
            update_mask: row.get::<_, Option<String>>("update_mask")?,
        })
    }

    /// Converts this row to the domain type.
    pub fn to_domain(&self) -> Result<UpdateUserRequest, ConversionError> {
        Ok(UpdateUserRequest {
            user: match &self.user { Some(s) => Some(serde_json::from_str(s)?), None => None },
            update_mask: serde_json::from_str(&self.update_mask.as_deref().unwrap_or("[]"))?,
        })
    }

    /// Converts this row into the domain type, consuming self.
    pub fn into_domain(self) -> Result<UpdateUserRequest, ConversionError> {
        Ok(UpdateUserRequest {
            user: match self.user { Some(s) => Some(serde_json::from_str(&s)?), None => None },
            update_mask: serde_json::from_str(&self.update_mask.as_deref().unwrap_or("[]"))?,
        })
    }

    /// Constructs a row from the domain type.
    pub fn from_domain(d: &UpdateUserRequest) -> Result<Self, ConversionError> {
        Ok(Self {
            user: match &d.user { Some(v) => Some(serde_json::to_string(v)?), None => None },
            update_mask: Some(serde_json::to_string(&d.update_mask)?),
        })
    }
}

impl TryFrom<&UpdateUserRequest> for UpdateUserRequestRow {
    type Error = ConversionError;
    fn try_from(d: &UpdateUserRequest) -> Result<Self, Self::Error> {
        Self::from_domain(d)
    }
}

impl TryFrom<UpdateUserRequestRow> for UpdateUserRequest {
    type Error = ConversionError;
    fn try_from(row: UpdateUserRequestRow) -> Result<Self, Self::Error> {
        row.into_domain()
    }
}


// Code generated by proto2type. DO NOT EDIT.
// source: service.proto

package gen

import (
	protovalidate "buf.build/go/protovalidate"
	context "context"
	pb "github.com/protocgen/proto2type/testdata/golden/go/pb"
)

// GetUserRequest is the domain representation of test.v1.GetUserRequest.
//
// GetUserRequest is the request for GetUser.
type GetUserRequest struct {
	ID string `json:"id,omitempty"`
}

// ToProto converts to the protobuf message.
func (g *GetUserRequest) ToProto() *pb.GetUserRequest {
	if g == nil {
		return nil
	}
	out := &pb.GetUserRequest{
		Id: g.ID,
	}
	return out
}

// FromProto populates from a protobuf message.
//
// When msg is non-nil, all proto-backed fields on the receiver are reset before
// population: reference fields (slices, maps, pointers, nested messages) are
// cleared to their zero values, and scalar fields are overwritten. Computed
// fields and storage-only fields (e.g. Firestore document IDs) are not touched.
// This makes it safe to reuse a receiver across multiple FromProto calls.
// When msg is nil, the receiver is left unchanged.
func (g *GetUserRequest) FromProto(msg *pb.GetUserRequest) {
	if msg == nil {
		return
	}
	g.ID = msg.Id
}

// ApplyFieldMaskGetUserRequest copies fields from src to dst based on the given paths.
//
// Only top-level field names are supported (e.g. "email", "address").
// Nested paths like "address.street" are silently ignored.
func ApplyFieldMaskGetUserRequest(dst, src *GetUserRequest, paths []string) {
	if dst == nil || src == nil {
		return
	}
	for _, path := range paths {
		switch path {
		case "id":
			dst.ID = src.ID
		}
	}
}

// Clone returns a deep copy of GetUserRequest.
func (g *GetUserRequest) Clone() *GetUserRequest {
	if g == nil {
		return nil
	}
	clone := &GetUserRequest{
		ID: g.ID,
	}
	return clone
}

// Equal reports whether g and other are equal.
func (g *GetUserRequest) Equal(other *GetUserRequest) bool {
	if g == other {
		return true
	}
	if g == nil || other == nil {
		return false
	}
	if g.ID != other.ID {
		return false
	}
	return true
}

// Validate checks domain invariants on GetUserRequest.
// It also runs buf.validate constraints via protovalidate.
func (g *GetUserRequest) Validate() error {
	if g == nil {
		return nil
	}
	if err := protovalidate.Validate(g.ToProto()); err != nil {
		return err
	}
	return nil
}

// GetUserResponse is the domain representation of test.v1.GetUserResponse.
//
// GetUserResponse is the response for GetUser.
type GetUserResponse struct {
	User *User `json:"user,omitempty"`
}

// ToProto converts to the protobuf message.
func (g *GetUserResponse) ToProto() *pb.GetUserResponse {
	if g == nil {
		return nil
	}
	out := &pb.GetUserResponse{}
	if g.User != nil {
		out.User = g.User.ToProto()
	}
	return out
}

// FromProto populates from a protobuf message.
//
// When msg is non-nil, all proto-backed fields on the receiver are reset before
// population: reference fields (slices, maps, pointers, nested messages) are
// cleared to their zero values, and scalar fields are overwritten. Computed
// fields and storage-only fields (e.g. Firestore document IDs) are not touched.
// This makes it safe to reuse a receiver across multiple FromProto calls.
// When msg is nil, the receiver is left unchanged.
func (g *GetUserResponse) FromProto(msg *pb.GetUserResponse) {
	if msg == nil {
		return
	}
	g.User = nil
	if msg.User != nil {
		g.User = &User{}
		g.User.FromProto(msg.User)
	}
}

// ApplyFieldMaskGetUserResponse copies fields from src to dst based on the given paths.
//
// Only top-level field names are supported (e.g. "email", "address").
// Nested paths like "address.street" are silently ignored.
func ApplyFieldMaskGetUserResponse(dst, src *GetUserResponse, paths []string) {
	if dst == nil || src == nil {
		return
	}
	for _, path := range paths {
		switch path {
		case "user":
			dst.User = src.User.Clone()
		}
	}
}

// Clone returns a deep copy of GetUserResponse.
func (g *GetUserResponse) Clone() *GetUserResponse {
	if g == nil {
		return nil
	}
	clone := &GetUserResponse{}
	if g.User != nil {
		clone.User = g.User.Clone()
	}
	return clone
}

// Equal reports whether g and other are equal.
func (g *GetUserResponse) Equal(other *GetUserResponse) bool {
	if g == other {
		return true
	}
	if g == nil || other == nil {
		return false
	}
	if (g.User == nil) != (other.User == nil) {
		return false
	}
	if g.User != nil && !g.User.Equal(other.User) {
		return false
	}
	return true
}

// Validate checks domain invariants on GetUserResponse.
// It also runs buf.validate constraints via protovalidate.
func (g *GetUserResponse) Validate() error {
	if g == nil {
		return nil
	}
	if err := protovalidate.Validate(g.ToProto()); err != nil {
		return err
	}
	return nil
}

// ListUsersRequest is the domain representation of test.v1.ListUsersRequest.
//
// ListUsersRequest is the request for ListUsers.
type ListUsersRequest struct {
	PageSize  int32  `json:"page_size,omitempty"`
	PageToken string `json:"page_token,omitempty"`
}

// ToProto converts to the protobuf message.
func (l *ListUsersRequest) ToProto() *pb.ListUsersRequest {
	if l == nil {
		return nil
	}
	out := &pb.ListUsersRequest{
		PageSize:  l.PageSize,
		PageToken: l.PageToken,
	}
	return out
}

// FromProto populates from a protobuf message.
//
// When msg is non-nil, all proto-backed fields on the receiver are reset before
// population: reference fields (slices, maps, pointers, nested messages) are
// cleared to their zero values, and scalar fields are overwritten. Computed
// fields and storage-only fields (e.g. Firestore document IDs) are not touched.
// This makes it safe to reuse a receiver across multiple FromProto calls.
// When msg is nil, the receiver is left unchanged.
func (l *ListUsersRequest) FromProto(msg *pb.ListUsersRequest) {
	if msg == nil {
		return
	}
	l.PageSize = msg.PageSize
	l.PageToken = msg.PageToken
}

// ApplyFieldMaskListUsersRequest copies fields from src to dst based on the given paths.
//
// Only top-level field names are supported (e.g. "email", "address").
// Nested paths like "address.street" are silently ignored.
func ApplyFieldMaskListUsersRequest(dst, src *ListUsersRequest, paths []string) {
	if dst == nil || src == nil {
		return
	}
	for _, path := range paths {
		switch path {
		case "page_size":
			dst.PageSize = src.PageSize
		case "page_token":
			dst.PageToken = src.PageToken
		}
	}
}

// Clone returns a deep copy of ListUsersRequest.
func (l *ListUsersRequest) Clone() *ListUsersRequest {
	if l == nil {
		return nil
	}
	clone := &ListUsersRequest{
		PageSize:  l.PageSize,
		PageToken: l.PageToken,
	}
	return clone
}

// Equal reports whether l and other are equal.
func (l *ListUsersRequest) Equal(other *ListUsersRequest) bool {
	if l == other {
		return true
	}
	if l == nil || other == nil {
		return false
	}
	if l.PageSize != other.PageSize {
		return false
	}
	if l.PageToken != other.PageToken {
		return false
	}
	return true
}

// Validate checks domain invariants on ListUsersRequest.
// It also runs buf.validate constraints via protovalidate.
func (l *ListUsersRequest) Validate() error {
	if l == nil {
		return nil
	}
	if err := protovalidate.Validate(l.ToProto()); err != nil {
		return err
	}
	return nil
}

// ListUsersResponse is the domain representation of test.v1.ListUsersResponse.
//
// ListUsersResponse is the response for ListUsers.
type ListUsersResponse struct {
	Users         []*User `json:"users,omitempty"`
	NextPageToken string  `json:"next_page_token,omitempty"`
}

// ToProto converts to the protobuf message.
func (l *ListUsersResponse) ToProto() *pb.ListUsersResponse {
	if l == nil {
		return nil
	}
	out := &pb.ListUsersResponse{
		NextPageToken: l.NextPageToken,
	}
	if len(l.Users) > 0 {
		out.Users = make([]*pb.User, len(l.Users))
		for i, v := range l.Users {
			if v != nil {
				out.Users[i] = v.ToProto()
			}
		}
	}
	return out
}

// FromProto populates from a protobuf message.
//
// When msg is non-nil, all proto-backed fields on the receiver are reset before
// population: reference fields (slices, maps, pointers, nested messages) are
// cleared to their zero values, and scalar fields are overwritten. Computed
// fields and storage-only fields (e.g. Firestore document IDs) are not touched.
// This makes it safe to reuse a receiver across multiple FromProto calls.
// When msg is nil, the receiver is left unchanged.
func (l *ListUsersResponse) FromProto(msg *pb.ListUsersResponse) {
	if msg == nil {
		return
	}
	l.Users = nil
	if len(msg.Users) > 0 {
		l.Users = make([]*User, len(msg.Users))
		for i, v := range msg.Users {
			if v != nil {
				elem := &User{}
				elem.FromProto(v)
				l.Users[i] = elem
			}
		}
	}
	l.NextPageToken = msg.NextPageToken
}

// ApplyFieldMaskListUsersResponse copies fields from src to dst based on the given paths.
//
// Only top-level field names are supported (e.g. "email", "address").
// Nested paths like "address.street" are silently ignored.
func ApplyFieldMaskListUsersResponse(dst, src *ListUsersResponse, paths []string) {
	if dst == nil || src == nil {
		return
	}
	for _, path := range paths {
		switch path {
		case "users":
			if src.Users != nil {
				dst.Users = make([]*User, len(src.Users))
				for i, v := range src.Users {
					dst.Users[i] = v.Clone()
				}
			} else {
				dst.Users = nil
			}
		case "next_page_token":
			dst.NextPageToken = src.NextPageToken
		}
	}
}

// Clone returns a deep copy of ListUsersResponse.
func (l *ListUsersResponse) Clone() *ListUsersResponse {
	if l == nil {
		return nil
	}
	clone := &ListUsersResponse{
		NextPageToken: l.NextPageToken,
	}
	if l.Users != nil {
		clone.Users = make([]*User, len(l.Users))
		for i, v := range l.Users {
			if v != nil {
				clone.Users[i] = v.Clone()
			}
		}
	}
	return clone
}

// Equal reports whether l and other are equal.
func (l *ListUsersResponse) Equal(other *ListUsersResponse) bool {
	if l == other {
		return true
	}
	if l == nil || other == nil {
		return false
	}
	if len(l.Users) != len(other.Users) {
		return false
	}
	for i := range l.Users {
		if (l.Users[i] == nil) != (other.Users[i] == nil) {
			return false
		}
		if l.Users[i] != nil && !l.Users[i].Equal(other.Users[i]) {
			return false
		}
	}
	if l.NextPageToken != other.NextPageToken {
		return false
	}
	return true
}

// Validate checks domain invariants on ListUsersResponse.
// It also runs buf.validate constraints via protovalidate.
func (l *ListUsersResponse) Validate() error {
	if l == nil {
		return nil
	}
	if err := protovalidate.Validate(l.ToProto()); err != nil {
		return err
	}
	return nil
}

// UpdateUserRequest is the domain representation of test.v1.UpdateUserRequest.
//
// UpdateUserRequest is the request for UpdateUser.
type UpdateUserRequest struct {
	User       *User    `json:"user,omitempty"`
	UpdateMask []string `json:"update_mask,omitempty"`
}

// ToProto converts to the protobuf message.
func (u *UpdateUserRequest) ToProto() *pb.UpdateUserRequest {
	if u == nil {
		return nil
	}
	out := &pb.UpdateUserRequest{
		UpdateMask: u.UpdateMask,
	}
	if u.User != nil {
		out.User = u.User.ToProto()
	}
	return out
}

// FromProto populates from a protobuf message.
//
// When msg is non-nil, all proto-backed fields on the receiver are reset before
// population: reference fields (slices, maps, pointers, nested messages) are
// cleared to their zero values, and scalar fields are overwritten. Computed
// fields and storage-only fields (e.g. Firestore document IDs) are not touched.
// This makes it safe to reuse a receiver across multiple FromProto calls.
// When msg is nil, the receiver is left unchanged.
func (u *UpdateUserRequest) FromProto(msg *pb.UpdateUserRequest) {
	if msg == nil {
		return
	}
	u.User = nil
	if msg.User != nil {
		u.User = &User{}
		u.User.FromProto(msg.User)
	}
	u.UpdateMask = nil
	u.UpdateMask = msg.UpdateMask
}

// ApplyFieldMaskUpdateUserRequest copies fields from src to dst based on the given paths.
//
// Only top-level field names are supported (e.g. "email", "address").
// Nested paths like "address.street" are silently ignored.
func ApplyFieldMaskUpdateUserRequest(dst, src *UpdateUserRequest, paths []string) {
	if dst == nil || src == nil {
		return
	}
	for _, path := range paths {
		switch path {
		case "user":
			dst.User = src.User.Clone()
		case "update_mask":
			if src.UpdateMask != nil {
				dst.UpdateMask = make([]string, len(src.UpdateMask))
				copy(dst.UpdateMask, src.UpdateMask)
			} else {
				dst.UpdateMask = nil
			}
		}
	}
}

// Clone returns a deep copy of UpdateUserRequest.
func (u *UpdateUserRequest) Clone() *UpdateUserRequest {
	if u == nil {
		return nil
	}
	clone := &UpdateUserRequest{}
	if u.UpdateMask != nil {
		clone.UpdateMask = make([]string, len(u.UpdateMask))
		copy(clone.UpdateMask, u.UpdateMask)
	}
	if u.User != nil {
		clone.User = u.User.Clone()
	}
	return clone
}

// Equal reports whether u and other are equal.
func (u *UpdateUserRequest) Equal(other *UpdateUserRequest) bool {
	if u == other {
		return true
	}
	if u == nil || other == nil {
		return false
	}
	if (u.User == nil) != (other.User == nil) {
		return false
	}
	if u.User != nil && !u.User.Equal(other.User) {
		return false
	}
	if len(u.UpdateMask) != len(other.UpdateMask) {
		return false
	}
	for i := range u.UpdateMask {
		if u.UpdateMask[i] != other.UpdateMask[i] {
			return false
		}
	}
	return true
}

// Validate checks domain invariants on UpdateUserRequest.
// It also runs buf.validate constraints via protovalidate.
func (u *UpdateUserRequest) Validate() error {
	if u == nil {
		return nil
	}
	if err := protovalidate.Validate(u.ToProto()); err != nil {
		return err
	}
	return nil
}

// UserServiceHandler — UserService provides CRUD operations on Users.
//
// All methods use domain types rather than proto types for clean
// separation between the transport layer and business logic.
type UserServiceHandler interface {
	// GetUser — GetUser retrieves a user by ID.
	GetUser(ctx context.Context, req *GetUserRequest) (*GetUserResponse, error)
	// ListUsers — ListUsers retrieves a paginated list of users.
	ListUsers(ctx context.Context, req *ListUsersRequest) (*ListUsersResponse, error)
	// UpdateUser — UpdateUser updates a user with field mask.
	UpdateUser(ctx context.Context, req *UpdateUserRequest) (*GetUserResponse, error)
}

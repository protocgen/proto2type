// Code generated by proto2type. DO NOT EDIT.
// source: service.proto
// backend: mongo

package gen

import (
	pb "github.com/protocgen/proto2type/testdata/golden/go/pb"
)

// GetUserRequestMongo is the MongoDB storage representation of test.v1.GetUserRequest.
type GetUserRequestMongo struct {
	ID string `bson:"id,omitempty"`
}

// ToProto converts to the protobuf message.
func (g *GetUserRequestMongo) ToProto() *pb.GetUserRequest {
	if g == nil {
		return nil
	}
	out := &pb.GetUserRequest{
		Id: g.ID,
	}
	return out
}

// FromProto populates from a protobuf message.
//
// When msg is non-nil, all proto-backed fields on the receiver are reset before
// population: reference fields (slices, maps, pointers, nested messages) are
// cleared to their zero values, and scalar fields are overwritten. Computed
// fields and storage-only fields (e.g. Firestore document IDs) are not touched.
// This makes it safe to reuse a receiver across multiple FromProto calls.
// When msg is nil, the receiver is left unchanged.
func (g *GetUserRequestMongo) FromProto(msg *pb.GetUserRequest) {
	if msg == nil {
		return
	}
	g.ID = msg.Id
}

// ToDomain converts to the domain type.
func (g *GetUserRequestMongo) ToDomain() *GetUserRequest {
	if g == nil {
		return nil
	}
	d := &GetUserRequest{
		ID: g.ID,
	}
	return d
}

// FromDomain populates from the domain type.
func (g *GetUserRequestMongo) FromDomain(d *GetUserRequest) {
	if d == nil {
		return
	}
	g.ID = d.ID
}

// GetUserResponseMongo is the MongoDB storage representation of test.v1.GetUserResponse.
type GetUserResponseMongo struct {
	User *UserMongo `bson:"user,omitempty"`
}

// ToProto converts to the protobuf message.
func (g *GetUserResponseMongo) ToProto() *pb.GetUserResponse {
	if g == nil {
		return nil
	}
	out := &pb.GetUserResponse{}
	if g.User != nil {
		out.User = g.User.ToProto()
	}
	return out
}

// FromProto populates from a protobuf message.
//
// When msg is non-nil, all proto-backed fields on the receiver are reset before
// population: reference fields (slices, maps, pointers, nested messages) are
// cleared to their zero values, and scalar fields are overwritten. Computed
// fields and storage-only fields (e.g. Firestore document IDs) are not touched.
// This makes it safe to reuse a receiver across multiple FromProto calls.
// When msg is nil, the receiver is left unchanged.
func (g *GetUserResponseMongo) FromProto(msg *pb.GetUserResponse) {
	if msg == nil {
		return
	}
	g.User = nil
	if msg.User != nil {
		g.User = &UserMongo{}
		g.User.FromProto(msg.User)
	}
}

// ToDomain converts to the domain type.
func (g *GetUserResponseMongo) ToDomain() *GetUserResponse {
	if g == nil {
		return nil
	}
	d := &GetUserResponse{}
	if g.User != nil {
		d.User = g.User.ToDomain()
	}
	return d
}

// FromDomain populates from the domain type.
func (g *GetUserResponseMongo) FromDomain(d *GetUserResponse) {
	if d == nil {
		return
	}
	if d.User != nil {
		g.User = &UserMongo{}
		g.User.FromDomain(d.User)
	}
}

// ListUsersRequestMongo is the MongoDB storage representation of test.v1.ListUsersRequest.
type ListUsersRequestMongo struct {
	PageSize  int32  `bson:"page_size,omitempty"`
	PageToken string `bson:"page_token,omitempty"`
}

// ToProto converts to the protobuf message.
func (l *ListUsersRequestMongo) ToProto() *pb.ListUsersRequest {
	if l == nil {
		return nil
	}
	out := &pb.ListUsersRequest{
		PageSize:  l.PageSize,
		PageToken: l.PageToken,
	}
	return out
}

// FromProto populates from a protobuf message.
//
// When msg is non-nil, all proto-backed fields on the receiver are reset before
// population: reference fields (slices, maps, pointers, nested messages) are
// cleared to their zero values, and scalar fields are overwritten. Computed
// fields and storage-only fields (e.g. Firestore document IDs) are not touched.
// This makes it safe to reuse a receiver across multiple FromProto calls.
// When msg is nil, the receiver is left unchanged.
func (l *ListUsersRequestMongo) FromProto(msg *pb.ListUsersRequest) {
	if msg == nil {
		return
	}
	l.PageSize = msg.PageSize
	l.PageToken = msg.PageToken
}

// ToDomain converts to the domain type.
func (l *ListUsersRequestMongo) ToDomain() *ListUsersRequest {
	if l == nil {
		return nil
	}
	d := &ListUsersRequest{
		PageSize:  l.PageSize,
		PageToken: l.PageToken,
	}
	return d
}

// FromDomain populates from the domain type.
func (l *ListUsersRequestMongo) FromDomain(d *ListUsersRequest) {
	if d == nil {
		return
	}
	l.PageSize = d.PageSize
	l.PageToken = d.PageToken
}

// ListUsersResponseMongo is the MongoDB storage representation of test.v1.ListUsersResponse.
type ListUsersResponseMongo struct {
	Users         []*UserMongo `bson:"users,omitempty"`
	NextPageToken string       `bson:"next_page_token,omitempty"`
}

// ToProto converts to the protobuf message.
func (l *ListUsersResponseMongo) ToProto() *pb.ListUsersResponse {
	if l == nil {
		return nil
	}
	out := &pb.ListUsersResponse{
		NextPageToken: l.NextPageToken,
	}
	if len(l.Users) > 0 {
		out.Users = make([]*pb.User, len(l.Users))
		for i, v := range l.Users {
			if v != nil {
				out.Users[i] = v.ToProto()
			}
		}
	}
	return out
}

// FromProto populates from a protobuf message.
//
// When msg is non-nil, all proto-backed fields on the receiver are reset before
// population: reference fields (slices, maps, pointers, nested messages) are
// cleared to their zero values, and scalar fields are overwritten. Computed
// fields and storage-only fields (e.g. Firestore document IDs) are not touched.
// This makes it safe to reuse a receiver across multiple FromProto calls.
// When msg is nil, the receiver is left unchanged.
func (l *ListUsersResponseMongo) FromProto(msg *pb.ListUsersResponse) {
	if msg == nil {
		return
	}
	l.Users = nil
	if len(msg.Users) > 0 {
		l.Users = make([]*UserMongo, len(msg.Users))
		for i, v := range msg.Users {
			if v != nil {
				elem := &UserMongo{}
				elem.FromProto(v)
				l.Users[i] = elem
			}
		}
	}
	l.NextPageToken = msg.NextPageToken
}

// ToDomain converts to the domain type.
func (l *ListUsersResponseMongo) ToDomain() *ListUsersResponse {
	if l == nil {
		return nil
	}
	d := &ListUsersResponse{
		NextPageToken: l.NextPageToken,
	}
	if len(l.Users) > 0 {
		d.Users = make([]*User, len(l.Users))
		for i, v := range l.Users {
			if v != nil {
				d.Users[i] = v.ToDomain()
			}
		}
	}
	return d
}

// FromDomain populates from the domain type.
func (l *ListUsersResponseMongo) FromDomain(d *ListUsersResponse) {
	if d == nil {
		return
	}
	if len(d.Users) > 0 {
		l.Users = make([]*UserMongo, len(d.Users))
		for i, v := range d.Users {
			if v != nil {
				elem := &UserMongo{}
				elem.FromDomain(v)
				l.Users[i] = elem
			}
		}
	}
	l.NextPageToken = d.NextPageToken
}

// UpdateUserRequestMongo is the MongoDB storage representation of test.v1.UpdateUserRequest.
type UpdateUserRequestMongo struct {
	User       *UserMongo `bson:"user,omitempty"`
	UpdateMask []string   `bson:"update_mask,omitempty"`
}

// ToProto converts to the protobuf message.
func (u *UpdateUserRequestMongo) ToProto() *pb.UpdateUserRequest {
	if u == nil {
		return nil
	}
	out := &pb.UpdateUserRequest{
		UpdateMask: u.UpdateMask,
	}
	if u.User != nil {
		out.User = u.User.ToProto()
	}
	return out
}

// FromProto populates from a protobuf message.
//
// When msg is non-nil, all proto-backed fields on the receiver are reset before
// population: reference fields (slices, maps, pointers, nested messages) are
// cleared to their zero values, and scalar fields are overwritten. Computed
// fields and storage-only fields (e.g. Firestore document IDs) are not touched.
// This makes it safe to reuse a receiver across multiple FromProto calls.
// When msg is nil, the receiver is left unchanged.
func (u *UpdateUserRequestMongo) FromProto(msg *pb.UpdateUserRequest) {
	if msg == nil {
		return
	}
	u.User = nil
	if msg.User != nil {
		u.User = &UserMongo{}
		u.User.FromProto(msg.User)
	}
	u.UpdateMask = nil
	u.UpdateMask = msg.UpdateMask
}

// ToDomain converts to the domain type.
func (u *UpdateUserRequestMongo) ToDomain() *UpdateUserRequest {
	if u == nil {
		return nil
	}
	d := &UpdateUserRequest{
		UpdateMask: u.UpdateMask,
	}
	if u.User != nil {
		d.User = u.User.ToDomain()
	}
	return d
}

// FromDomain populates from the domain type.
func (u *UpdateUserRequestMongo) FromDomain(d *UpdateUserRequest) {
	if d == nil {
		return
	}
	if d.User != nil {
		u.User = &UserMongo{}
		u.User.FromDomain(d.User)
	}
	u.UpdateMask = d.UpdateMask
}

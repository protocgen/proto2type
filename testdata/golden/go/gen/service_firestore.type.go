// Code generated by proto2type. DO NOT EDIT.
// source: service.proto
// backend: firestore

package gen

import (
	pb "github.com/protocgen/proto2type/testdata/golden/go/pb"
)

// GetUserRequestFirestore is the Firestore storage representation of test.v1.GetUserRequest.
type GetUserRequestFirestore struct {
	ID string `firestore:"id,omitempty"`
}

// ToProto converts to the protobuf message.
func (g *GetUserRequestFirestore) ToProto() *pb.GetUserRequest {
	if g == nil {
		return nil
	}
	out := &pb.GetUserRequest{
		Id: g.ID,
	}
	return out
}

// FromProto populates from a protobuf message.
//
// When msg is non-nil, all proto-backed fields on the receiver are reset before
// population: reference fields (slices, maps, pointers, nested messages) are
// cleared to their zero values, and scalar fields are overwritten. Computed
// fields and storage-only fields (e.g. Firestore document IDs) are not touched.
// This makes it safe to reuse a receiver across multiple FromProto calls.
// When msg is nil, the receiver is left unchanged.
func (g *GetUserRequestFirestore) FromProto(msg *pb.GetUserRequest) {
	if msg == nil {
		return
	}
	g.ID = msg.Id
}

// ToDomain converts to the domain type.
func (g *GetUserRequestFirestore) ToDomain() *GetUserRequest {
	if g == nil {
		return nil
	}
	d := &GetUserRequest{
		ID: g.ID,
	}
	return d
}

// FromDomain populates from the domain type.
func (g *GetUserRequestFirestore) FromDomain(d *GetUserRequest) {
	if d == nil {
		return
	}
	g.ID = d.ID
}

// GetUserResponseFirestore is the Firestore storage representation of test.v1.GetUserResponse.
type GetUserResponseFirestore struct {
	User *UserFirestore `firestore:"user,omitempty"`
}

// ToProto converts to the protobuf message.
func (g *GetUserResponseFirestore) ToProto() *pb.GetUserResponse {
	if g == nil {
		return nil
	}
	out := &pb.GetUserResponse{}
	if g.User != nil {
		out.User = g.User.ToProto()
	}
	return out
}

// FromProto populates from a protobuf message.
//
// When msg is non-nil, all proto-backed fields on the receiver are reset before
// population: reference fields (slices, maps, pointers, nested messages) are
// cleared to their zero values, and scalar fields are overwritten. Computed
// fields and storage-only fields (e.g. Firestore document IDs) are not touched.
// This makes it safe to reuse a receiver across multiple FromProto calls.
// When msg is nil, the receiver is left unchanged.
func (g *GetUserResponseFirestore) FromProto(msg *pb.GetUserResponse) {
	if msg == nil {
		return
	}
	g.User = nil
	if msg.User != nil {
		g.User = &UserFirestore{}
		g.User.FromProto(msg.User)
	}
}

// ToDomain converts to the domain type.
func (g *GetUserResponseFirestore) ToDomain() *GetUserResponse {
	if g == nil {
		return nil
	}
	d := &GetUserResponse{}
	if g.User != nil {
		d.User = g.User.ToDomain()
	}
	return d
}

// FromDomain populates from the domain type.
func (g *GetUserResponseFirestore) FromDomain(d *GetUserResponse) {
	if d == nil {
		return
	}
	if d.User != nil {
		g.User = &UserFirestore{}
		g.User.FromDomain(d.User)
	}
}

// ListUsersRequestFirestore is the Firestore storage representation of test.v1.ListUsersRequest.
type ListUsersRequestFirestore struct {
	PageSize  int32  `firestore:"page_size,omitempty"`
	PageToken string `firestore:"page_token,omitempty"`
}

// ToProto converts to the protobuf message.
func (l *ListUsersRequestFirestore) ToProto() *pb.ListUsersRequest {
	if l == nil {
		return nil
	}
	out := &pb.ListUsersRequest{
		PageSize:  l.PageSize,
		PageToken: l.PageToken,
	}
	return out
}

// FromProto populates from a protobuf message.
//
// When msg is non-nil, all proto-backed fields on the receiver are reset before
// population: reference fields (slices, maps, pointers, nested messages) are
// cleared to their zero values, and scalar fields are overwritten. Computed
// fields and storage-only fields (e.g. Firestore document IDs) are not touched.
// This makes it safe to reuse a receiver across multiple FromProto calls.
// When msg is nil, the receiver is left unchanged.
func (l *ListUsersRequestFirestore) FromProto(msg *pb.ListUsersRequest) {
	if msg == nil {
		return
	}
	l.PageSize = msg.PageSize
	l.PageToken = msg.PageToken
}

// ToDomain converts to the domain type.
func (l *ListUsersRequestFirestore) ToDomain() *ListUsersRequest {
	if l == nil {
		return nil
	}
	d := &ListUsersRequest{
		PageSize:  l.PageSize,
		PageToken: l.PageToken,
	}
	return d
}

// FromDomain populates from the domain type.
func (l *ListUsersRequestFirestore) FromDomain(d *ListUsersRequest) {
	if d == nil {
		return
	}
	l.PageSize = d.PageSize
	l.PageToken = d.PageToken
}

// ListUsersResponseFirestore is the Firestore storage representation of test.v1.ListUsersResponse.
type ListUsersResponseFirestore struct {
	Users         []*UserFirestore `firestore:"users,omitempty"`
	NextPageToken string           `firestore:"next_page_token,omitempty"`
}

// ToProto converts to the protobuf message.
func (l *ListUsersResponseFirestore) ToProto() *pb.ListUsersResponse {
	if l == nil {
		return nil
	}
	out := &pb.ListUsersResponse{
		NextPageToken: l.NextPageToken,
	}
	if len(l.Users) > 0 {
		out.Users = make([]*pb.User, len(l.Users))
		for i, v := range l.Users {
			if v != nil {
				out.Users[i] = v.ToProto()
			}
		}
	}
	return out
}

// FromProto populates from a protobuf message.
//
// When msg is non-nil, all proto-backed fields on the receiver are reset before
// population: reference fields (slices, maps, pointers, nested messages) are
// cleared to their zero values, and scalar fields are overwritten. Computed
// fields and storage-only fields (e.g. Firestore document IDs) are not touched.
// This makes it safe to reuse a receiver across multiple FromProto calls.
// When msg is nil, the receiver is left unchanged.
func (l *ListUsersResponseFirestore) FromProto(msg *pb.ListUsersResponse) {
	if msg == nil {
		return
	}
	l.Users = nil
	if len(msg.Users) > 0 {
		l.Users = make([]*UserFirestore, len(msg.Users))
		for i, v := range msg.Users {
			if v != nil {
				elem := &UserFirestore{}
				elem.FromProto(v)
				l.Users[i] = elem
			}
		}
	}
	l.NextPageToken = msg.NextPageToken
}

// ToDomain converts to the domain type.
func (l *ListUsersResponseFirestore) ToDomain() *ListUsersResponse {
	if l == nil {
		return nil
	}
	d := &ListUsersResponse{
		NextPageToken: l.NextPageToken,
	}
	if len(l.Users) > 0 {
		d.Users = make([]*User, len(l.Users))
		for i, v := range l.Users {
			if v != nil {
				d.Users[i] = v.ToDomain()
			}
		}
	}
	return d
}

// FromDomain populates from the domain type.
func (l *ListUsersResponseFirestore) FromDomain(d *ListUsersResponse) {
	if d == nil {
		return
	}
	if len(d.Users) > 0 {
		l.Users = make([]*UserFirestore, len(d.Users))
		for i, v := range d.Users {
			if v != nil {
				elem := &UserFirestore{}
				elem.FromDomain(v)
				l.Users[i] = elem
			}
		}
	}
	l.NextPageToken = d.NextPageToken
}

// UpdateUserRequestFirestore is the Firestore storage representation of test.v1.UpdateUserRequest.
type UpdateUserRequestFirestore struct {
	User       *UserFirestore `firestore:"user,omitempty"`
	UpdateMask []string       `firestore:"update_mask,omitempty"`
}

// ToProto converts to the protobuf message.
func (u *UpdateUserRequestFirestore) ToProto() *pb.UpdateUserRequest {
	if u == nil {
		return nil
	}
	out := &pb.UpdateUserRequest{
		UpdateMask: u.UpdateMask,
	}
	if u.User != nil {
		out.User = u.User.ToProto()
	}
	return out
}

// FromProto populates from a protobuf message.
//
// When msg is non-nil, all proto-backed fields on the receiver are reset before
// population: reference fields (slices, maps, pointers, nested messages) are
// cleared to their zero values, and scalar fields are overwritten. Computed
// fields and storage-only fields (e.g. Firestore document IDs) are not touched.
// This makes it safe to reuse a receiver across multiple FromProto calls.
// When msg is nil, the receiver is left unchanged.
func (u *UpdateUserRequestFirestore) FromProto(msg *pb.UpdateUserRequest) {
	if msg == nil {
		return
	}
	u.User = nil
	if msg.User != nil {
		u.User = &UserFirestore{}
		u.User.FromProto(msg.User)
	}
	u.UpdateMask = nil
	u.UpdateMask = msg.UpdateMask
}

// ToDomain converts to the domain type.
func (u *UpdateUserRequestFirestore) ToDomain() *UpdateUserRequest {
	if u == nil {
		return nil
	}
	d := &UpdateUserRequest{
		UpdateMask: u.UpdateMask,
	}
	if u.User != nil {
		d.User = u.User.ToDomain()
	}
	return d
}

// FromDomain populates from the domain type.
func (u *UpdateUserRequestFirestore) FromDomain(d *UpdateUserRequest) {
	if d == nil {
		return
	}
	if d.User != nil {
		u.User = &UserFirestore{}
		u.User.FromDomain(d.User)
	}
	u.UpdateMask = d.UpdateMask
}

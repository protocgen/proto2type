// Code generated by proto2type. DO NOT EDIT.
// source: user/v1/user.proto
/* eslint-disable @typescript-eslint/no-explicit-any, @typescript-eslint/no-empty-interface */
import { z } from "zod";

/** UserRole defines the access level for a user. */
export type UserRole = "USER_ROLE_UNSPECIFIED" | "USER_ROLE_MEMBER" | "USER_ROLE_ADMIN" | (string & {});
// Note: Numeric enum values should be converted to strings before passing to this schema.
export const UserRoleSchema = /* @__PURE__ */ z.enum(["USER_ROLE_UNSPECIFIED", "USER_ROLE_MEMBER", "USER_ROLE_ADMIN"]).or(z.string());

/** User represents a registered user. */
export interface User {
  /** Email must be a valid email address. */
  email: string;
  /** Display name: 1–100 characters. */
  displayName: string;
  /** Age: 13–120. */
  age: number;
  /** Role must be a defined enum value. */
  role: UserRole;
  /** Bio is optional, max 500 chars when provided. */
  bio?: string | null;
}
export const UserSchema = /* @__PURE__ */ z.object({
  /** Email must be a valid email address. */
  email: z.string().refine(v => v === "" || z.string().email().safeParse(v).success, { message: "must be a valid email" }).default(""),
  /** Display name: 1–100 characters. */
  displayName: z.string().refine(v => [...v].length >= 1, { message: "must be at least 1 characters" }).refine(v => [...v].length <= 100, { message: "must be at most 100 characters" }).default(""),
  /** Age: 13–120. */
  age: z.number().int().gte(13).lte(120).default(0),
  /** Role must be a defined enum value. */
  role: UserRoleSchema.default("USER_ROLE_UNSPECIFIED"),
  /** Bio is optional, max 500 chars when provided. */
  bio: z.string().refine(v => [...v].length <= 500, { message: "must be at most 500 characters" }).nullish(),
});

